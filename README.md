# Jupyterlab applet

This applet create a Jupyter Lab from any container as long as jupyter lab is installed in it. GPU support can be activated from input option if needed.

The current project data will be visible in `~/project_data` folder.

Inputs:

- `docker_image`: path to a docker image file or dockerhub image
- `use_gpu`: boolean to activate GPU support

